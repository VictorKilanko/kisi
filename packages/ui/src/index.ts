/**
 * @kisi/ui — shared React components for the Kisi universe.
 *
 * The bird portraits live here so every site (kisi.africa, kisifarm, kisikids)
 * draws the exact same image for a given bird, straight from that bird's canon
 * colours. One bird, one recognisable face, everywhere.
 */
export { ChickenPortrait } from "./ChickenPortrait";
